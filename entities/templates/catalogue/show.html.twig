{% extends 'base.html.twig' %}

{% block title %}Catalogue{% endblock %}

{% block body %}
    <h1>Catalogue</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ catalogue.id }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ catalogue.url }}</td>
            </tr>
            <tr>
                <th>Created_date</th>
                <td>{{ catalogue.createdDate ? catalogue.createdDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_catalogue_index') }}">back to list</a>

    <a href="{{ path('app_catalogue_edit', {'id': catalogue.id}) }}">edit</a>

    {{ include('catalogue/_delete_form.html.twig') }}
{% endblock %}
